doctype html
head
  script(src='http://www.google.com/jsapi')
  script.
    // Load the Visualization API and the piechart package.
      google.load('visualization', '1.0', {'packages':['corechart', 'bar']});

      // Set a callback to run when the Google Visualization API is loaded.
      google.setOnLoadCallback(drawChart);

      // Callback that creates and populates a data table, 
      // instantiates the pie chart, passes in the data and
      // draws it.
      function drawChart() {

      // Create the data table.
      var data = google.visualization.arrayToDataTable([
        ['City', 'Promoter-Dectractors', 'Cumulative', 'Percent'],
        ['Trump', #{trump_p}, #{trump_c}, #{trump_per}],
        ['Clinton', #{clinton_p}, #{clinton_c}, #{clinton_per}]
      ]);

      // Set chart options
      var options = {
        title: 'Sentiment Value of Candidates',
        chartArea: {width: '50%'},
        hAxis: {
          title: 'Points',
          minValue: 0
        },
        vAxis: {
          title: 'Candidates'
        }
      };

      // Instantiate and draw our chart, passing in some options.
      var chart = new google.visualization.BarChart(document.getElementById('chart_div'));
      chart.draw(data, options);
    }
  title
      h1 Trump v. Clinton
body
  h1 Trump v. Clinton
    h3 (2016 Presidential Candidates)
  #chart_div(style='width:400; height:300')